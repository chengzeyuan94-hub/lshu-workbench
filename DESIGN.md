# L叔工作台 · DESIGN.md

> 状态：**Active / V2.1**  
> 生效日期：2026-08-24  
> 适用范围：`frontend/` 全部页面与共享组件  
> 视觉基准：[`docs/design-v2/reference-command-white-orange-yellow.png`](docs/design-v2/reference-command-white-orange-yellow.png)  
> 方法来源：[OpenDesign](https://github.com/nexu-io/open-design) 的 `DESIGN.md` 品牌合同思路 + [Design OS](https://github.com/buildermethods/design-os) 的“规划 → 设计系统 → 分区设计 → 交付”流程。

本文件是 L叔工作台前端视觉的**唯一事实来源**。当参考图、旧 CSS、页面内联样式、历史截图或开发者临时判断与本文件冲突时，以本文件为准。

---

## 1. 产品与设计目标

L叔工作台是一个本地优先的个人工作指挥台，汇集桌面文件、Things、飞书、小红书创作者数据、热点素材与知识库。

新版视觉不是游戏界面，也不是普通 SaaS 后台。它应像一张由办公打印机输出的、干净克制的个人指挥面板：

- **黑白极简**：纯白页面、纯黑结构和文字。
- **双色专色**：橙色负责行动与选中，黄色负责提醒与优先级。
- **像素秩序**：像素网格图标、模块编号、等宽状态码、精准对齐。
- **打印终端感**：细线、裁角、表格式信息、任务队列和状态栏。
- **高可用性**：中文正文清晰，信息密度高但不拥挤，交互状态明确。
- **L叔个性**：IP 形象保留，但以克制的黑白方式出现，不抢占工作信息。

### 1.1 不可破坏的产品约束

- 不改变现有路由、API、数据结构、请求时机和业务 handler。
- 不删除现有功能、状态、错误提示、空状态、抽屉或筛选能力。
- 不伪造日历、待办、指标或连接状态；只展示真实已有数据。
- 不触碰后端、小红书/OpenCLI、次幂、知识库、Things、飞书与本地数据库逻辑。
- 本轮允许调整前端 DOM 结构、组件抽象、样式类和可访问性属性。

---

## 2. 设计治理

### 2.1 事实来源优先级

1. 本 `DESIGN.md`
2. V2 视觉基准图
3. 已通过验收的 V2 页面截图
4. `frontend/src/styles/tokens.css`
5. 组件与页面实现
6. V1 历史截图与旧 Neubrutalism 样式

### 2.2 Design OS 执行流程

每次改造一个页面或组件时，必须依次完成：

1. **确认职责**：该页面帮助用户完成什么工作。
2. **确认数据**：只使用页面当前已有的数据和状态。
3. **套用系统**：只使用本文件定义的 token、组件和交互规则。
4. **覆盖状态**：默认、hover、focus、active、loading、empty、error、disabled。
5. **构建验证**：TypeScript 构建通过，多尺寸无横向溢出。
6. **视觉验收**：与视觉基准对照，记录偏差后再交付。

### 2.3 技术选型边界

- [8bitcn/ui](https://github.com/theorcdev/8bitcn-ui) 用作可访问交互模式与像素组件造型参考。
- 当前项目不是 Tailwind/shadcn 工程，首版**不整套安装 8bitcn**；应在现有 React + CSS 中吸收其模式。
- [Pixel UI](https://github.com/maomentai817/pixel-ui) 是 Vue 3 + CSS Houdini 项目，只参考像素边框和主题参数，不作为运行时依赖。
- 图标可参考 [Pixelarticons](https://github.com/halfmage/pixelarticons) 的 24×24 整数网格规则；如引入包，必须锁定版本并保留许可。
- 不使用远程 CDN 字体、运行时在线主题或会导致离线失效的视觉依赖。

---

## 3. 核心视觉原则

### 3.1 视觉比例

整屏视觉占比应大致满足：

- 白色：`78%–84%`
- 黑色：`12%–18%`
- 橙色：`≤ 4%`
- 黄色：`≤ 2%`

橙黄不是大面积背景色。除当前导航项和唯一主行动按钮外，强调色只允许以细条、描边、角标、文字或小面积状态块出现。

### 3.2 禁止项

- 禁止灰色、米色、奶油色、暖白色的页面或卡片底色。
- 禁止纸张纹理、颗粒、噪点、半调网背景、扫描线与做旧质感。
- 禁止渐变、玻璃拟态、模糊阴影、发光、金属质感。
- 禁止大圆角、胶囊卡片、满屏卡片套卡片。
- 禁止蓝、绿、红、粉、棕、紫等第三种强调色。
- 禁止用 emoji 充当 UI 图标或状态图标。
- 禁止用全像素字体显示长段中文正文。
- 禁止为了贴近效果图而新增假的业务数据或假的可点击功能。

---

## 4. 设计令牌

令牌必须定义在 `frontend/src/styles/tokens.css`，页面不得硬编码颜色、阴影、圆角和常用间距。

### 4.1 颜色

| Token | 值 | 用途 |
| --- | --- | --- |
| `--ui-white` | `#FFFFFF` | 页面、模块、卡片、输入框唯一实体底色 |
| `--ui-black` | `#111111` | 主文字、图标、边框、数据线 |
| `--ui-orange` | `#FF5A1F` | 当前导航、选中态、主按钮、错误/高风险 |
| `--ui-yellow` | `#FFC928` | 优先级、提醒、待处理、今日标记 |
| `--ui-overlay` | `rgba(17, 17, 17, 0.52)` | 抽屉遮罩；不可作为模块底色 |

次级文字继续使用 `--ui-black`，通过 `opacity: 0.62` 弱化，不创建灰色实体色板。

### 4.2 语义映射

| 语义 | 表达方式 |
| --- | --- |
| 默认/正常 | 黑色图标或实心黑点 + 文本 |
| 当前/选中/主行动 | 橙色 |
| 高优先级/提醒/待确认 | 黄色 |
| 错误/高风险 | 橙色描边 + 错误图标 + 明确文字，不只靠颜色 |
| 成功/已完成 | 黑色勾选图标 + “已完成”文字，不使用绿色 |
| 禁用 | 黑色 `opacity: 0.38`，白底不变 |
| 多数据系列 | 黑 / 橙 / 黄 + 实线、虚线、点纹差异 |

### 4.3 兼容别名

迁移期允许保留旧变量名，但它们只能映射到新令牌：

```css
--bg: var(--ui-white);
--bg-soft: var(--ui-white);
--ink: var(--ui-black);
--ink-soft: var(--ui-black);
--brown: var(--ui-orange);
--brown-light: var(--ui-orange);
--olive: var(--ui-black);
--olive-light: var(--ui-yellow);
--denim: var(--ui-black);
--denim-light: var(--ui-yellow);
--blush: var(--ui-orange);
--blush-light: var(--ui-yellow);
--red: var(--ui-orange);
--red-light: var(--ui-yellow);
--success: var(--ui-black);
--warning: var(--ui-yellow);
--danger: var(--ui-orange);
```

兼容别名只用于防止首轮回归；新代码必须直接使用新的语义 token。

### 4.4 间距

采用 4px 基础网格：

| Token | 值 |
| --- | --- |
| `--space-1` | `4px` |
| `--space-2` | `8px` |
| `--space-3` | `12px` |
| `--space-4` | `16px` |
| `--space-5` | `24px` |
| `--space-6` | `32px` |
| `--space-7` | `48px` |

### 4.5 边框、裁角与阴影

| Token | 值 |
| --- | --- |
| `--line-thin` | `1px solid var(--ui-black)` |
| `--line-strong` | `2px solid var(--ui-black)` |
| `--line-accent` | `2px solid var(--ui-orange)` |
| `--radius-control` | `0px` |
| `--notch-sm` | `6px` |
| `--notch-md` | `10px` |
| `--shadow-action` | `2px 2px 0 var(--ui-black)` |

- 普通分组优先用留白和 `1px` 分隔线。
- 只有一级模块、抽屉和主按钮可用 `2px` 描边。
- 一级模块可用统一裁角；列表行、输入框和普通按钮保持直角。
- 除主按钮外不使用阴影。
- 不在交互元素本身使用会裁掉 focus ring 的 `clip-path`；裁角应由外层或伪元素完成。

---

## 5. 字体系统

### 5.1 字体栈

生产默认像素字体为 **Fusion Pixel 12px Proportional**（简体中文 + Latin 子集），在本仓库本地托管为 `LShu Pixel`。

- 来源：[TakWolf/fusion-pixel-font](https://github.com/TakWolf/fusion-pixel-font) 发布 `2026.08.11`
- 文件：`frontend/src/assets/fonts/fusion-pixel-12px-proportional-latin.otf.woff2` 与 `fusion-pixel-12px-proportional-zh_hans.otf.woff2`
- 许可证：SIL Open Font License 1.1（`frontend/src/assets/fonts/OFL.txt`，以及 zip 内 Ark Pixel / Cubic 11 / Galmuri 的捆绑许可）
- 参考页 Pixel UI 使用 Zpix 的视觉气质，但 **Zpix 不作为生产默认字体**（商业授权受限）。
- 禁止运行时从 CDN、GitHub 或第三方网站加载字体。

```css
--font-pixel: "LShu Pixel", ui-monospace, "SFMono-Regular", Menlo, Monaco, monospace;
--font-display: var(--font-pixel);
--font-body: "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", system-ui, sans-serif;
--font-mono: var(--font-pixel);
```

像素字体适用范围：品牌标题、导航标签与 `H-01` / `T-02` 等编号、页面 H1、模块短标题、按钮 / Tab / Chip / Badge、时间日期、数据指标与短状态码。

继续使用 `--font-body` 的范围：长段说明、笔记标题和正文、飞书聊天摘要、错误提示、表单输入内容、待办详情和知识库正文。

像素字体元素使用 `font-weight: 400` 与 `font-synthesis: none`，不以浏览器合成粗体建立层级。字号均为整数像素；正文不低于 13px。不把像素字体覆盖到所有中文正文，也不改变 `--font-body`。

首页时钟使用电脑系统时区 `Intl.DateTimeFormat().resolvedOptions().timeZone`，不读取项目 `settings.timezone`，也不默认 `Asia/Shanghai`。系统时区只用于时间计算，不作为城市名展示。天气在用户明确授权后通过 Geolocation 读取当前位置；经纬度仅取两位小数并用于天气与城市级反向地理解析，不写入数据库、不回传页面、不记录日志。城市解析失败时显示「电脑当前位置」，绝不回退为上海；位置数据标注 OpenStreetMap 来源。

### 5.2 字阶

| 层级 | 字号 / 行高 | 字重 | 用途 |
| --- | --- | --- | --- |
| Display | `48px / 1` | 400 | 时间或唯一主数据（像素字体，禁止合成粗体） |
| H1 | `30px / 1.2` | 400 | 页面标题 |
| H2 | `20px / 1.3` | 400 | 一级模块标题 |
| H3 | `16px / 1.4` | 400 | 子区标题 |
| Body | `15px / 1.65` | 400–600 | 中文正文（系统字体） |
| Small | `13px / 1.5` | 400 | 辅助信息 |
| Micro | `13px / 1.35` | 400 | 模块编号、更新时间；不足 13px 的旧 micro 仅用于非正文编号 |
| Data | `28–40px / 1` | 400 | 指标和数量 |

正文不得低于 13px；主要操作不得低于 14px。

---

## 6. 图标与 L叔 IP

### 6.1 图标

- 默认 `24×24`，小图标 `16×16`。
- 路径落在整数网格，避免半像素坐标。
- 使用 `stroke-linecap="square"`、`stroke-linejoin="miter"`。
- 线宽仅允许 `2px`；小图标可用 `1.5px`。
- 默认黑色；选中项可反白；只有提醒小标记可用橙或黄。
- 所有图标必须有文本标签或可访问名称。

### 6.2 L叔 IP

- 品牌区使用派生组件 `frontend/src/components/AvatarMascot.tsx`，源文件备份为 `frontend/src/assets/avatar/lshu-avatar.source.svg`。
- 原始 iCloud SVG 只读，不写回；运行时不依赖 `file://` 绝对路径。
- 头像只显示脑袋：去掉最外层橙色背景圆，使用紧凑 viewBox；白底、黑色细框，槽位至少 `44×44`。
- 单屏只出现一次（左上角品牌区），首页不再重复放置。
- 眼睛跟随与星星眼为装饰动画；`prefers-reduced-motion: reduce` 时关闭跟随，星星仅静态显示。

---

## 7. 应用壳层

### 7.1 桌面布局

- `.shell` 填满视口，纯白背景。
- 左侧导航宽度建议 `248px`，允许在 `224–264px` 内响应式收缩。
- 左侧导航与主内容之间使用 `2px` 黑色竖线，不使用阴影。
- 主内容 `flex: 1; min-width: 0; max-width: none`，保持铺满浏览器剩余宽度。
- 主内容水平 padding：1440px 及以上 `32px`；1280px `24px`；1024px `16px`。
- 页面顶端不再使用彩色条纹；改为状态头、细分隔线或模块编号。

### 7.2 导航编号

| 页面 | 路由 | 编号 |
| --- | --- | --- |
| 今日 | `/` | `H-01` |
| 待办 | `/todos` | `T-02` |
| 内容表现 | `/performance` | `C-03` |
| 热点雷达 | `/hotspots` | `R-04` |
| 知识大脑 | `/knowledge` | `K-05` |
| 扫描报告 | `/scan` | `S-06` |
| 设置 | `/settings` | `S-07` |

- 默认项：白底、黑字、1px 黑框。
- 当前项：橙底，高对比文字，2px 黑色外框；不使用阴影。
- hover：黄底或黄色左条，不产生位移动画。
- focus-visible：`3px` 橙色 outline，`2px` offset。

### 7.3 底部状态条

桌面端允许使用一条轻量状态条，展示已有信息，例如本地运行、同步状态或刷新状态。它不能新增虚假的控制项；没有真实 handler 的内容只能是只读状态，不可伪装为按钮。

---

## 8. 核心组件合同

### 8.1 一级模块 `ui-module`

- 纯白底、2px 黑框、可选统一裁角。
- 标题行高 40px 左右，底部 1px 分隔线。
- 标题包含模块编号、名称与真实数量/更新时间。
- 模块之间 16–24px 间距。
- 禁止模块内再堆同样重量的卡片。

### 8.2 数据行 `ui-row`

- 默认白底，行间 1px 黑线。
- hover 用黄色 `4px` 左条或黄色局部标记，不填满整行。
- selected 用橙色 `4px` 左条 + 2px 橙色外框。
- 行高至少 48px，点击目标至少 44×44px。
- 长路径、标题和 URL 必须换行或省略，不得撑破布局。

### 8.3 按钮

- Primary：橙底、黑字、2px 黑框、2px 硬边阴影。
- Secondary：白底、黑字、2px 黑框。
- Attention：黄底、黑字、2px 黑框。
- Ghost：白底、1px 黑框或仅下划线；不可做胶囊。
- hover 只改变底色或反色；active 位移最多 1px。
- disabled 保留白底，整体透明度 0.38。
- 同一视图只允许一个视觉主按钮。

### 8.4 徽章与状态

- 使用矩形小标签，不用 `999px` 胶囊。
- 默认白底黑框；优先级可用黄底；高风险可用橙色边框。
- 状态必须同时包含文字或图标，不能只靠颜色。

### 8.5 输入与选择控件

- 纯白底、1–2px 黑框、0 圆角。
- focus-visible 用橙色外轮廓。
- placeholder 使用黑色并降低 opacity。
- checkbox、switch、radio 需要清晰的键盘焦点和 44px 点击区域。
- 开关不使用绿色；开启态使用橙色或黑色实心。

### 8.6 Tabs / Chips

- 视觉上是同一条分段控制，不是多枚漂浮胶囊。
- active：橙色底或橙色下边线。
- 非 active：白底黑字。
- 数量直接跟随文本，不额外堆徽章。

### 8.7 表格

- 首选表格/数据行，而不是每条数据一张卡片。
- 表头白底、黑色粗字、底部 2px 黑线。
- 行 hover 使用黄色细条；选中使用橙色细条。
- 数字列使用 `font-variant-numeric: tabular-nums`。
- 窄屏允许横向滚动，但页面本身不得横向溢出。

### 8.8 抽屉和弹层

- 遮罩使用 `--ui-overlay`。
- 抽屉纯白底、左侧 2px 黑线、0 圆角、无阴影。
- 标题区固定，关闭按钮 44×44px，支持 Escape。
- 内容内部用分区线，不使用卡片嵌套。

### 8.9 图表

- 背景纯白，坐标轴和文字黑色。
- 主系列橙色，次系列黄色，第三系列黑色。
- 超过三系列时复用颜色并增加实线/虚线/点纹，不引入新颜色。
- tooltip 纯白底、2px 黑框、0 圆角、无模糊阴影。
- 30 天密集趋势必须保持日期可辨识并允许横向滚动。

### 8.10 提示、空状态与错误

- 不使用 emoji。
- 空状态：黑色像素图标 + 简短说明 + 至多一个操作。
- 提醒：黄色左条 + 黑字。
- 错误：橙色左条/描边 + 错误图标 + 文字和修复动作。
- 成功：黑色勾选 + 明确成功文案。

---

## 9. 页面级设计合同

### 9.1 今日 `H-01`

目标：一眼知道今天最重要的工作、工作来源与账号状态。

- 保留扫描桌面、同步小红书、待办、指标、趋势、最近笔记全部现有功能。
- 页面头改为状态头，不再使用大暖色 Hero 卡。
- L叔头像缩小为辅助识别，不占据主要信息宽度。
- 待办优先使用任务队列表格；指标优先使用紧凑数据单元。
- 趋势和最近笔记使用一级模块，不再每项一张带阴影卡片。
- 不新增页面当前拿不到的日历数据；日历规划能力在待办页呈现。

### 9.2 待办 `T-02`

目标：从 Desktop / Things / 飞书收集任务，确认、排程并核对完成状态。

- 连接器状态改为一行状态台或紧凑网格。
- 智能收件箱、今日计划等视图使用分段 Tabs。
- 待办列表改为任务队列；来源、优先级、时长、状态对齐成列。
- 详情抽屉、证据、排程预览和所有现有操作必须保留。
- 自动排程与自动确认完成开关必须清楚显示默认状态和写入风险。
- 橙色用于当前任务/主操作，黄色用于待确认/无法安排提醒。

### 9.3 内容表现 `C-03`

目标：在工作台内完成账号指标、趋势、内容表现和单篇诊断浏览。

- 指标卡改为紧凑数据格；不为每个指标使用不同颜色。
- 表现趋势使用黑/橙/黄和线型区分。
- 笔记列表以表格为主，收藏率/低表现通过文本 + 黄/橙标记表达。
- 单篇诊断抽屉必须保留全部原始字段与数据来源说明。

### 9.4 热点雷达 `R-04`

目标：同步虎嗅/36Kr 等素材，筛选、阅读、精选并生成内容草稿。

- 实时素材/精选使用单一分段 Tabs。
- 筛选器形成一条清晰工具栏，窄屏可换行。
- 素材列表采用表格/新闻队列，不做彩色卡片墙。
- 正文和精选抽屉保持纯白阅读面，使用细分隔线。
- 评分采用黑色数字、黄色候选、橙色风险，不扩展色板。

### 9.5 知识大脑 `K-05`

目标：浏览知识库、问答、核对来源并管理文档。

- 聊天区是主工作面，文档管理为次级模块。
- 用户/助手消息通过对齐、边框和标签区分，不使用不同底色气泡。
- 来源引用采用编号数据行，保持可追溯性。
- 上传、刷新、删除等风险操作必须有明确文字与确认状态。

### 9.6 扫描报告 `S-06`

目标：查看桌面扫描历史、项目簇和文件证据。

- 左侧扫描历史使用选中橙条；右侧详情使用表格和分区线。
- 文件类型不再使用多色块；统一黑框标签，以文本区分类型。
- 文件路径允许换行/省略并支持 title 提示。

### 9.7 设置 `S-07`

目标：查看和维护所有本地连接、账号、同步计划与隐私边界。

- 按“系统 / 小红书 / 热点 / 知识库 / 待办连接器 / 隐私”分区。
- 在线、离线、未配置、错误必须使用图标 + 文本，不靠红绿灯颜色。
- 错误区使用橙色边框，警告使用黄色左条。
- 表单控件统一为纯白底黑框，保存主按钮橙色。

---

## 10. 响应式规则

必须验收：`2560×1000`、`1920×1080`、`1440×900`、`1280×800`、`1024×768`、`390×844`，并额外检查 `1101px` 与 `1099px` 断点两侧。

### ≥ 1200px

- 左侧固定导航 + 主内容区。
- 主要工作区可使用 `minmax(0, 1.6fr) minmax(320px, 0.8fr)` 两列。
- 页面填满剩余宽度，不设置导致页面偏左的固定 `max-width`。

### 768–1199px

- 导航可转顶部横向栏或保持窄侧栏。
- 两列模块按内容重要性重排为单列。
- 表格自身可滚动，页面不横向滚动。

### < 768px

- 导航成为可横向滚动的顶部/底部导航。
- 所有一级模块单列，取消装饰性裁角。
- 页面 padding 12–16px。
- 主操作保持可见，次级状态条可折叠。
- 不隐藏完成核心流程所需的按钮、筛选和数据来源。

---

## 11. 动效与交互反馈

- 默认时长 `100–140ms`，仅用于颜色、边框和 1px 位移。
- 禁止卡片浮起、弹簧动画、持续闪烁、扫描线移动或模拟屏幕刷新。
- loading 使用黑色像素 spinner 或文字状态，不使用彩色骨架屏。
- 尊重 `prefers-reduced-motion: reduce`，关闭非必要过渡。

---

## 12. 可访问性

- 正文与背景对比度满足 WCAG AA。
- 所有交互控件支持键盘和 `:focus-visible`。
- 点击目标至少 `44×44px`。
- 状态不只依赖颜色，必须有文字、图标或纹理辅助。
- 图标按钮必须有 `aria-label`；装饰图标使用 `aria-hidden="true"`。
- Tabs、Dialog/Drawer、表格与表单使用正确语义。
- 不移除浏览器默认能力而不提供等价替代。

---

## 13. 工程实现合同

### 13.1 允许修改

- `frontend/src/styles/tokens.css`
- `frontend/src/styles/global.css`
- `frontend/src/components/AppShell.tsx`
- `frontend/src/components/icons.tsx`
- `frontend/src/components/widgets.tsx`
- `frontend/src/components/NoteDetailDrawer.tsx`
- `frontend/src/pages/*.tsx` 的视觉结构、类名、语义和可访问性属性
- `frontend/index.html`（仅本地字体声明或必要 meta）

### 13.2 禁止修改

- `backend/**`
- `frontend/src/api/client.ts`
- `frontend/src/types.ts`，除非仅为现有数据增加不改变运行时的类型收窄
- 路由路径、API 路径、事件 handler 的业务含义
- `.env*`、数据库、凭证、OpenCLI、Things、飞书或知识库配置

### 13.3 CSS 迁移原则

- 第一阶段保留 `.nb-*` 旧类名作为兼容层，视觉实现改为 V2；可新增 `.ui-*` 语义类。
- 逐步把 JSX 中的颜色、圆角、背景和阴影内联样式迁移到 CSS 类。
- 完成后页面中不得出现 `#fffdf7`、旧品牌色或 `borderRadius: 999` 等旧视觉硬编码。
- 新增样式必须使用 token；不得在页面组件中继续扩散硬编码视觉值。
- 不引入 CSS-in-JS。

---

## 14. 验收清单

### 14.1 视觉

- [ ] 页面和所有模块实体底色均为纯白。
- [ ] 全站只出现黑、白、橙、黄四种实体颜色。
- [ ] 无灰色背景质感、纸纹、噪点、渐变、模糊阴影。
- [ ] 橙黄总占比克制，橙 ≤ 4%，黄 ≤ 2%。
- [ ] 一级模块使用统一细线/裁角语法。
- [ ] 无大圆角卡片和胶囊标签。
- [ ] 图标为一致的像素网格风，不含 UI emoji。
- [ ] L叔 IP 每屏最多一次，且不压过工作信息。

### 14.2 功能回归

- [ ] 7 个路由全部可进入。
- [ ] 所有 API 请求、按钮、筛选、Tabs、抽屉和表单行为保持可用。
- [ ] loading、empty、error、disabled、hover、focus、active 全部有状态。
- [ ] 待办同步、确认、忽略、排程预览与详情证据不回归。
- [ ] 小红书同步、指标切换、趋势周期、笔记诊断不回归。
- [ ] 热点同步、筛选、分页、详情、精选和生成草稿不回归。
- [ ] 知识库问答、来源、文档管理不回归。
- [ ] 设置保存、连接测试和离线提示不回归。

### 14.3 构建与布局

- [ ] `npm run build` 通过，无 TypeScript 错误。
- [ ] 2560、1920、1440、1280、1024、390 六个视口无页面级横向溢出。
- [ ] 1101px / 1099px 断点切换无裁切或突变溢出。
- [ ] 长中文标题、路径、URL、30 天趋势和抽屉内容不截断核心信息。
- [ ] 键盘可遍历所有主要操作，focus ring 清晰可见。
- [ ] `prefers-reduced-motion` 生效。

---

## 15. 设计决策记录

### 2026-08-24 · V2 视觉重构

- 选定第三种“彩墨指挥终端”模块结构。
- 去除灰色电子纸底、纸张纹理和多色状态面板。
- 改为纯白背景、纯黑结构、橙色与黄色少量点缀。
- 保留打印终端的裁角、模块编号、任务队列和状态条。
- OpenDesign `DESIGN.md` 作为品牌合同；Design OS 作为实施与验收流程。
- 首轮重构保持业务逻辑、API 和路由不变，由 Cursor 执行前端视觉迁移。
